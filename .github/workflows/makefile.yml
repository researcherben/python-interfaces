name: Makefile CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: python version
      uses: actions/perfect-python
      run: python3 --version

    - name: Run check
      run: cd completed_script/ && make black
